# Database (used by packages/database — Prisma)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/finance_tracker?schema=public"

# Backend (apps/backend — NestJS)
PORT=3000
NODE_ENV=development
